spring:
  application:
    name: costlydiet
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:8084}/${POSTGRES_DB:costlydiet}
    username: ${POSTGRES_USER:admin}
    password: ${POSTGRES_PASSWORD:pwd}
    driver-class-name: org.postgresql.Driver

